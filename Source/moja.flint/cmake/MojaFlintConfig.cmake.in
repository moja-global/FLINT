get_filename_component(MojaFlint_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

list(APPEND CMAKE_MODULE_PATH ${MojaFlint_CMAKE_DIR})

find_dependency(Boost 1.70 REQUIRED COMPONENTS log)

list(REMOVE_AT CMAKE_MODULE_PATH -1)

if(NOT TARGET Moja::Flint)
    include("${JSONUtils_CMAKE_DIR}/MojaFlintTargets.cmake")
endif()

set(MojaFlint_LIBRARIES Moja::Flint)